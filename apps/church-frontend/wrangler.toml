name = "churchos-church-frontend"
main = "apps/church-frontend/src/app.tsx"
compatibility_date = "2026-05-28"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm install && npm run build"

[site]
bucket = "public"
entry-point = "apps/church-frontend"
